{
  "name": "bookmyshow-clone",
  "private": true,
  "scripts": {
    "start": "npm run dev",
    "dev": "concurrently -k -n api,web -c magenta,cyan \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && .venv/bin/uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend && backend/.venv/bin/pip install -r backend/requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
